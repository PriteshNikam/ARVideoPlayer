package com.example.arvideoplayer

import androidx.appcompat.widget.AppCompatImageView
import androidx.appcompat.widget.AppCompatTextView

interface ARViewCallback {
    // fun updateUIOnVideoCompletion()
    fun initializeVideoPlayback()
    // fun markTopicWatched(imageView: AppCompatImageView, textView: AppCompatTextView)
}